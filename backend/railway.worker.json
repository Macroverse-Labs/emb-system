{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "Dockerfile" },
  "deploy": {
    "startCommand": "sh -c 'celery -A app.tasks worker -l info'",
    "restartPolicyType": "ON_FAILURE"
  }
}
